<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.liheji.server.mapper.AccountMapper">
    <!-- mapper 使用缓存的方式是使用”<cache-ref>“标签引用dao的命名空间，因为dao中已经开启缓存，-->
    <!-- 这样与dao中的缓存保持一直，如果mapper文件不引用缓存，-->
    <!-- 执行update方法时，不会清除dao的缓存，导致数据库数据与缓存数据不一致。-->
    <!-- 如果直接使用”<cache>“标签开启缓存，会与dao中的冲突，服务启动失败。-->

    <cache-ref namespace="top.liheji.server.mapper.AccountMapper"/>

    <resultMap id="BaseResultMap" type="top.liheji.server.pojo.Account">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="mobile" column="mobile"/>
        <result property="email" column="email"/>
        <result property="isEnabled" column="is_enabled"/>
        <result property="isSuperuser" column="is_superuser"/>
        <result property="lastLogin" column="last_login"/>
        <result property="updateTime" column="update_time"/>
        <result property="createTime" column="create_time"/>
        <result property="version" column="version"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,username,password,
        mobile,email,is_enabled,is_superuser,
        last_login,update_time,create_time,
        version
    </sql>
</mapper>
